---
description: "<!-- One sentence. What this command does. Shown in command picker; routing truth. -->"
---

<!-- Creation scaffold only. Commands are short — fill direct, no separate README. -->

<!-- Imperative dense prose or a numbered list when order matters. No headers, no output
     contract section — this body IS the contract. State what to do, in what order, under
     what conditions. Handle `$ARGUMENTS` explicitly: what a value means, what absence means.
     Reference skills by behavior keyword, not hard name. Target: fits one screen. -->

<!-- Multi-step command with real delegation tradeoffs: add one `Delegate: allow|encourage|
     prohibit — reason` line per step. Skip entirely on a short/simple command. -->

<!-- Apply dense, compressed, low-filler phrasing throughout — the terse register a user gets
     by asking for fewer tokens or a briefer reply. -->
