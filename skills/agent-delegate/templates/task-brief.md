# Task brief

<!-- Copy, fill core, delete every optional block left empty. Dense fragments. Exact paths, commands, error strings verbatim. -->

## Core — six required fields

**Goal:** <outcome, not activity>
**Why:** <what this unblocks; lets worker resolve small choices in the right direction>
**Scope:** in — <files, paths, question slice>; out — <explicit exclusions; the anti-runoff half>
**Return:** <exact shape expected, plus mandatory `status:` and `gap:` fields>
**Done when:** <observable condition>
**Escalate when:** <trigger to stop and report instead of grinding>

## Context

<!-- Omit if none. Worker has zero history. Facts it cannot derive from the files. -->

- <fact — why it matters>

## Files of interest

<!-- Omit if none. Path alone causes blind scanning; always give the reason. -->

- `path:line` — <why this location matters>

## Do / don't

<!-- Omit if none. Only constraints the worker would otherwise violate. -->

- Do: <required approach>
- Don't: <forbidden approach — why>

## Checklist

<!-- Omit if none. Use only when order or completeness matters. -->

- [ ] <step>

## Acceptance checks

<!-- Omit if none. Observable verification the worker runs before reporting. -->

- <command or grep> → <expected result>
