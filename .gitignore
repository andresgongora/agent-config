# Skills
# no-ai-slop — upstream has no license yet; not redistributable until resolved
skills/no-ai-slop/

# Disabled skills (local experiments)
disabled-skills/
skills/opencode-local/

# Local model aliases (machine-specific)
model-aliases.json

# Plans
.agent/plan*

# agent-benchmark data (reports, prompts, run artifacts — never committed)
agents/agent-benchmark.md
skills/agent-benchmark/
/agent-benchmark/

# Frontier (machine-local state, not committed)
.agent/frontier.md

# In-review (not ready to commit)
commands/checkpoint.md
commands/commit.md
commands/document.md
